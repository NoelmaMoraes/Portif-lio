function exemplo1(){
    //return [1,2,3].map(x => x * 2); // [2,4,6] map => transformar *mesmo tamanho  *cada item vira outro

    //return [1,2,3,4].filter(x => x % 2 === 0); //[2,4] filter retorna somente os que passan no test

    //return [1,3,5,8].find(x => x % 2 === 0) // 8 find retorna o primeiro que satisfaz

    //return [2,4,6].every(x => x % 2 === 0) // true | every validar (bolean) todos precisam pasar

    //return [1,2,3].reduce ((acc, x) => acc + x, 0) // 6 |reduce = acomular (qualquer coisa) *transforma o array em um valor unico

    //pesquisar oque é calback
}

console.log(exemplo1());